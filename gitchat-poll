while true; do
  current=`git rev-parse HEAD`
  ./gitchat-receive >/dev/null
  after=`git rev-parse HEAD`
  if [ $current != $after ]; then
    git log --oneline $current..$after
    echo -e "\a"
  fi
  sleep 1
done
